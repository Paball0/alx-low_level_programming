#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H

/*
 * Header file defines macro ABS(x)
 *       and computes the absolute value of a number x.
 */

#define ABS(x) ((x) < 0 ? (-x) : (x))

#endif
